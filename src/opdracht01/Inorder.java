package opdracht01;

import java.util.*;

public class Inorder {
	
	 TreeNode root;
	 
	
	public Inorder(){
		Stack st = new Stack();
		current = root;
		while (current != null || st.isEmpty()) 
			if ()
			{
				// zelf aanvullen, 2 regels
			} 
			else 
			{
			// zelf aanvullen, 3 regels
			}
	}
}
